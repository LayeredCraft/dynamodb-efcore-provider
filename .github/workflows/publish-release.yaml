name: Publish Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: write
    uses: LayeredCraft/devops-templates/.github/workflows/publish-release.yml@76a2269c95c0f17eaac80d3020c783ff10be4371
    with:
      solution: EntityFrameworkCore.DynamoDb.slnx
      dotnetVersion: "10.0.x"   # fallback only — overridden when buildConfiguration=auto
      buildConfiguration: auto
      hasTests: true

  push:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: LayeredCraft/devops-templates/.github/actions/nuget-push@76a2269c95c0f17eaac80d3020c783ff10be4371
        with:
          nuget_user: ${{ secrets.NUGET_USER }}
