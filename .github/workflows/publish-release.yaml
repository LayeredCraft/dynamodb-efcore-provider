name: Publish Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: write
    uses: LayeredCraft/devops-templates/.github/workflows/publish-release.yml@fa7aa89e829a2b7bb947f5c38ba456189831534a
    with:
      solution: EntityFrameworkCore.DynamoDb.slnx
      dotnetVersion: "10.0.x"   # fallback only — overridden when buildConfiguration=auto
      buildConfiguration: auto
      hasTests: true

  push:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: LayeredCraft/devops-templates/.github/actions/nuget-push@fa7aa89e829a2b7bb947f5c38ba456189831534a
        with:
          nuget_user: ${{ secrets.NUGET_USER }}
